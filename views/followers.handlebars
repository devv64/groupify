
<div>
    <h1 id="name">{{username}}'s' Followers</h1>
    <section>
        {{#if followers}}
            <ul>
                {{#each followers}}
                <li>
                    <a href="/users/{{username}}">
                        {{this.username}}
                    </a>
                </li>
                {{/each}}
            </ul>
        {{else}}
            <p>No followers yet</p>
        {{/if}}
    </section>
    <a href="/users/{{username}}" class="backToProfile">Go back to profile page</a>
</div>

