
{{!-- TODO : 
notifications for likes, posts, and comments
display posts liked ; 
display comments 
make sure everything works from when viewing another profile
input validation
styling--}}


<div>
    <h1 id="name">{{username}}</h1>
    <img src="{{profilePic}}" alt="Profile Picture">
    <div class="manage">
        {{#if isPersonalAccount}}
            <a href="/users/{{username}}/manage" class="manage">Manage Profile Account</a>
            <a href="/users/{{username}}/delete" class="delete">Delete Post</a>
        {{else}}
            <div id="buttonContainer">
                <button type="submit" class="{{followClass}}" id="followingButton">{{followingText}}</button>
            </div>
        {{/if}}
    </div>

    <section class="followers" >
        <a href="/users/{{username}}/followers" id="followers">
            {{followers.length}} Followers
        </a>
    </section>

    <section class="following">
        <a href="/users/{{username}}/following">
            {{following.length}} Following
        </a>
    </section>

    <section class="posts">
        <h2>Posts</h2>
        {{#if posts}}
            {{#each posts}}
            <p>
                {{this.body}}
            </p>
            {{/each}}
        {{else}}
        <p>No posts yet</p>
        {{/if}}
    </section>

    <section class="likedPosts">
        <h2>Posts {{username}} liked</h2>
        {{#if likedPosts}}
            <ul>
                {{#each likedPosts}}
                <li>
                    {{this.body}} 
                </li>
                {{/each}}
            </ul>
        {{else}}
            <p>No liked posts yet</p>
        {{/if}}
    </section>
</div>